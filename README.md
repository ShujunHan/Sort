#### 插入排序
```
算法思路
   从第二个开始一直跟前面的数字进行比较  
1、如果后面的数字大于前面的数字， 就给前面的数字赋值并且就跳出循环
2、如果后面的数字小于前面的数字， 就把前面的数字赋值到后面数字的位置       
```
#### 选择排序
```
算法思路
最开始选择最小的数放在前面    
第一轮排序之后就从第二位数开始找最小的
第二轮排序之后就从第三位数开始找最小的，
一直找到倒数第一位数因为最后一次肯定是自己最大
因为在for循环 j参数的时候J+1所以只要判断小于ArrayList.lenth-1就行
```
